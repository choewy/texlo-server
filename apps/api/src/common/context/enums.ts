export enum ContextKey {
  RequestMethod = 'REQUEST_METHOD',
  RequestURL = 'REQUEST_URL',
  RequestIpAddress = 'REQUEST_IP_ADDRESS',
  User = 'USER',
  ContextName = 'CONTEXT_NAME',
}

export enum ContextUserSessionType {
  NONE = 'none',
  OMS = 'oms',
  WMS = 'wms',
}
