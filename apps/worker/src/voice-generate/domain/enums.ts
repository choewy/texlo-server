export enum VoiceProvider {
  Typecast = 'typecast',
  Supertone = 'supertone',
}

export enum VoiceGenerateStatus {
  Pending = 'pending',
  InProgress = 'in-progress',
  Failed = 'failed',
  Completed = 'completed',
}
