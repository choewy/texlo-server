export enum VideoDownloadPlatform {
  Youtube = 'youtube',
  Vimeo = 'vimeo',
}

export enum VideoDownloadStatus {
  Pending = 'pending',
  InProgress = 'in-progress',
  Failed = 'failed',
  Completed = 'completed',
}
